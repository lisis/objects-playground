// number, string, boolean, function, array

//** info sobre un michi en progats */
// En la primera posición tenemos el nombre del gato, en la segund su edad y en la tercerca si está vacunado o no
let michi1 = ["Bigotes", 3, true];

// si el michi cumple años
michi1[1] = michi1[1] + 1;
console.log(michi1);

/**
 * Los objetos son el tipo de estructura de dato adecuado para almacenar de forma muy explícita información sobre algo que quieres representar del mundo real
 */

let michiObjeto = {
    nombre: "Bigotes",
    edad: 3,
    estaVacunado: true
};

let michi2 = {
    nombre: "Mauricio",
    edad: 10,
    estaVacunado: false
};